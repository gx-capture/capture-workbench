{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the Capture Workbench verification window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
