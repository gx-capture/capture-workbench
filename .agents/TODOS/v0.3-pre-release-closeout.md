# Capture Workbench v0.3 Pre-release Closeout TODO

## v0.3.5 release-gate recovery

- [x] Preserve immutable `v0.3.2`, `v0.3.3`, and `v0.3.4` tags/runs and record
      the exact `v0.3.4` installed-size failure without claiming publication.
  Verify: read-only remote tag/run/Release inspection.

- [x] Make native-command PowerShell blocks fail fast, split installed-size
      smoke into a standalone CI/release step, and preserve explicit custom
      handling for expected nonzero ancestry/workflow lookup commands.
  Verify: focused desktop workflow contract test.

- [x] Require concrete installed-size/startup evidence and expose recursive
      nested smoke causes without changing process cleanup ownership.
  Verify: focused Python size-validator and installed-smoke tests.

- [x] Make executable catalog selection explicit so core builds cannot consume
      a stale generated production catalog.
  Verify: workflow contract plus a successful startup measurement.

- [x] Synchronize all live first-party release owners at `0.3.5`, rename the
      active checklist, and preserve older versions only as historical evidence
      or third-party dependency versions.
  Verify: `pnpm verify:release-version -- v0.3.5`, canonical classifier, and
  exact owner scan.

- [x] Run the exact local staged-runtime, NSIS, installed-size, and size-budget
      path. Stop and report if safe cleanup cannot be proven.
  Verify: standalone smoke output and size report.

- [x] Complete focused checks, one full `pnpm verify`, and
      diff/secret/generated audits.
  Verify: recorded command output plus unstaged tree evidence.

- [x] Obtain independent review of the exact prospective tree before any
      commit or push.
  Verify: reviewer verdict bound to the prospective tree SHA.

- [x] Restore clean default runtime verification dependency ownership after
      PR #12 CI run `30542133653` exposed missing Pillow/PDFium dev packages.
  Verify: isolated default locked sync; runtime version/lint/typecheck/test;
  core executable boundary without optional extras.

- [ ] Follow up separately on CI latency: split/parallelize independent lanes,
      improve cache/artifact reuse, and tier PR/main/release while retaining
      every mandatory correctness gate.
  Baseline: PR `22m20s`; main `20m40s`; installer/size about 8-10 minutes,
  desktop about 4m45s, optional workers about 2m40s.

## Completed foundation

- [x] Force the canonical release source lock to LF without normalizing
      unrelated JSON.
  Verify: `git check-attr text eol -- packages/capture-runtime/model-sources/release-model-source-lock.json`.

- [x] Reproduce a Windows-style fresh checkout in a real temporary Git
      repository and prove the checked-out lock remains canonical and
      classifies as core-only.
  Verify: focused `capture-runtime:test` source-lock test.

- [x] Add an early CI canonical release-metadata gate and synchronize live
      release owners while retaining occupied-version failure evidence.
  Verify: workflow inspection and the release-version checker.

- [x] Complete focused, full-workspace, release-build, diff, binary/generated,
      and secret audits before independent review.
  Verify: focused tests, `pnpm verify`,
  `pnpm nx run capture-runtime:build-release-artifacts`, and Git audits.

- [x] Synchronize `0.3.2`, organization metadata, schema bytes/hash, and Tauri
      identifier after recording public `v0.3.0` evidence.
  Verify: owner scan, release-version check, schema sync, focused schema tests.

- [x] Fix the packed Angular manifest contract and add tarball content,
      identity, dependency, integrity, and cert-prep-style Angular fixture
      coverage.
  Verify: `capture-angular:lint`, `test`, `pack`,
  `clean-consumer-smoke`.

- [x] Isolate the Workbench UI with Shadow DOM and verify CSS variable behavior
      plus outside-page sentinels.
  Verify: focused Angular tests and packed Vanilla browser smoke.

- [x] Harden cross-bundle custom-element registration, ownership conflict,
      retry, race cleanup, and property bridging.
  Verify: focused registration tests and package test.

- [x] Upgrade Angular, Vanilla, React, and Vue packed fixtures to random-port
      Playwright browser mounts with deterministic cleanup.
  Verify: `capture-angular:clean-consumer-smoke`.

- [x] Make release publication accept and preflight the NSIS installer, keep
      candidate assets in draft through npm publication, verify downloaded
      bytes twice, and fail closed for public releases.
  Verify: `node --test tools/publish-release.test.ts`.

- [x] Run CI on `develop`, reject tags outside `main` before setup, and run the
      complete verification/release build baseline.
  Verify: workflow inspection and package QA tests.

- [x] Make library updates journaled/recoverable, add filesystem failure
      injection/reopen tests, and reject deletion of active/runtime-linked
      documents in UI, store, and Rust.
  Verify: Workbench store tests plus desktop Cargo tests.

- [x] Replace renderer byte-array IPC with native dialog/drag-drop paths and a
      canonicalized, bounded, signature-checked, redacted Rust import command.
  Verify: renderer tests, Rust security tests, exact/oversized 50 MiB tests, and
  source-path scan of serialized responses/evidence.

- [x] Complete README/release checklist documentation and record automated,
      manual, unavailable, and production-build evidence without claiming
      unrun gates.
  Verify: docs scan, `git diff --check`, `corepack pnpm verify`, release runtime
  build, pack/consumer smoke, and NSIS build where locally feasible.
