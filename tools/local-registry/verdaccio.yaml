storage: ./storage
listen: 127.0.0.1:4873

uplinks:
  npmjs:
    url: https://registry.npmjs.org/

packages:
  '@gx-capture/*':
    access: $all
    publish: $all
  '**':
    access: $all
    publish: $all
    proxy: npmjs

log:
  - { type: stdout, format: pretty, level: http }
