export { CAPTURE_RUNTIME_MAJOR } from './runtime';

/** Feature-local limits and failure identifiers. */
export const MAX_INSTALLATIONS_PER_USER_ACTION = 16;
export const HOST_PROVIDER_FAILURE_CODE = 'host_provider_failed';
export const HOST_RECONCILIATION_FAILURE_CODE = 'host_reconciliation_unavailable';

export { DEFAULT_CAPTURE_WORKBENCH_CONFIG } from './workbench';
