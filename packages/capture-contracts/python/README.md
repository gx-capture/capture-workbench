# capture-contracts

This wheel contains generated Capture Workbench wire-contract Pydantic models,
JSON schemas, version constants, and provenance invariants. The Capture Runtime
remains the canonical semantic validator; hosts must submit final candidates to
the runtime before persisting them.
