# capture-contracts

This wheel contains generated Capture Workbench wire-contract schemas,
version constants, and provenance invariants. The Capture Runtime remains the
canonical Pydantic validator; this package deliberately does not claim to
validate or persist host data by itself.
